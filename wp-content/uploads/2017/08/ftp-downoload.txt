import  subprocess
import os
print('введите Полное название католога в виде C:\Intel ')
while True:
 l= input()
 directory=(l)
 files=os.listdir(directory)
 for i in files:
    print(i)
 file=os.chdir(l)
 #запуск программ или переход в другую директорию
 print ("введите название файла чтобы его открыть, \nдля того чтобы создать файл введите newf, \nвведите newd чтобы создать папку, \nвведите dir чтобы перейти в другой каталог либо в католог в этйо папке \nвведите del если хотите удалить файл в катологе>",l)
 c=input()
 if c=='dir':
     print(' введите Полное название католога в виде C:\Intel, если хотите открыть папку в той деректории где находитесь просто введите ее в ввиде windows ')
 elif c=='del': # Удаление файла из дерриктории
      d= input('Введите путь к файлу для удаления>>')
      os.remove(d,  dir_fd=None)
      print('готово,файл', d, 'удален из дерриктории:',l)
 elif c=='newf':
     nf=input('Введите имя и расширение файла>>')
     open (nf,"w")
     print('Файл создан в дерриктории:', l, 'с именем', nf)
 elif c=='newd':
     print('Введите название паки чтобы создать ее в дериктории:',l)
     nd=input(">>")
     os.mkdir(nd)
     print('готово, папка создана с именем-',nd,'в дерриктории:',l)
 else:
    cmd=(c)
    subprocess.Popen(cmd, shell = True)

